/* 
Crie uma função que recebe dois parâmetris, base e expoente, e retorne a base elevada ao expoente
*/

const potencia = function(base, expoente) {
    return Math.pow(base, expoente)
}

console.log(potencia(4, 2))